# Migrations

Place Claude’s schema here as:

```text
001_initial_schema.sql
```

The agent uses this file to implement API routes with correct table/column names.

Apply the same SQL in the Supabase dashboard **SQL Editor** if you haven’t already.
